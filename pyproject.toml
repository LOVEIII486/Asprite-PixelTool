[project]
name = "gittest"
version = "0.1.0"
description = "一个用于练习 Git / GitHub 工作流的 Python 示例项目"
readme = "README.md"
requires-python = ">=3.14"
authors = [{ name = "LOVEIII486" }]
keywords = ["git", "github", "workflow", "example"]
classifiers = [
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3 :: Only",
]
dependencies = []
